<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oyama">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oyama">

        <!-- Existing Activities -->
        <activity android:name=".DepotDetailsActivity" />
        <activity android:name=".SecondaryActivity" />
        <activity android:name=".QrScannerActivity" />
        <activity android:name=".QrResultActivity" />

        <!-- Main Activity -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- New Manual Entry Activity -->
        <activity android:name=".ManualEntryActivity" />

        <!-- New Success Activity -->
        <activity android:name=".SuccessActivity" />

    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>
